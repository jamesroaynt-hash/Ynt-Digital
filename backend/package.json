{
  "name": "ynt-dashboard-backend",
  "version": "1.0.0",
  "description": "YNT Digital Marketing Internal Dashboard — Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node -e \"const Database = require('./db/client'); const { initializeDatabase } = require('./db/init'); const db = new Database('./db/ynt.db'); db.pragma('journal_mode = WAL'); db.pragma('foreign_keys = ON'); initializeDatabase(db); db.close();\"",
    "reset-db": "node -e \"const fs = require('fs'); try { fs.rmSync('./db/ynt.db', { force: true }); } catch {}\" && npm run seed"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
